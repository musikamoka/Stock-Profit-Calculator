# 股价盈亏实验室

解压整个 ZIP，双击 start.bat。安装了 Python 3 时自动启动本机服务、选择空闲端口并打开浏览器，无需 pip 安装。按 Ctrl+C 停止服务。

没有 Python 3 也能一键打开离线计算页面，或直接双击 dist/index.html；行情接口需要 Python 3 服务。

## 使用

- 填写当前股价、每股建仓成本、持仓股数或投入金额。投入金额不含手续费，允许小数股。
- 选择持仓币种；切换币种只更改金额单位，不做汇率换算。所有价格和手续费必须使用同一币种。
- 手续费填写预计买入与卖出合计金额。净盈亏包括这笔固定费用。
- 鼠标在图表移动时，高亮竖线与下方数据联动；点击锁定，再次点击解锁。触屏可拖动。
- 输入精确目标价，或查看涨跌情景表。图表会扩展以容纳保本价及目标价。
- 页面会尝试记住输入；浏览器限制本地存储时仍可计算。同一打开地址才能读取其存储，因此 BAT 每次随机端口启动时不保证恢复上次输入。
- 默认 50、45、100 等均为演示输入，并非实际行情。

## 计算口径

股数 = 输入股数，或投入金额 ÷ 每股建仓成本。
净盈亏 =（目标价 − 成本价）× 股数 − 总手续费。
收益率 = 净盈亏 ÷（成本价 × 股数 + 总手续费）。
保本价 = 成本价 + 总手续费 ÷ 股数。
比现在多赚 / 少赚 =（目标价 − 当前价）× 股数。

这是普通股票多头持仓计算，不含税费、汇率、融资成本，不是期权定价模型。

## 预留行情接口

GET /api/quote?symbol=AAPL

在 quote_provider.py 中实现 get_quote(symbol)，按文件内约定返回 symbol、price、currency、asOf、source。尚未接入时返回 HTTP 501，网页提示手动输入，绝不回退到假行情。

网页提供 window.stockQuoteProvider.getQuote(symbol)，也可替换这一适配器。响应币种与持仓币种不符时不会覆盖当前价。API 密钥放在后端，避免泄漏给浏览器。

## 文件

start.bat：Windows 一键启动。
server.py：仅监听 127.0.0.1 的 Python 3 服务。
quote_provider.py：行情接入点。
dist/index.html、dist/app.js：无 CDN、无外部依赖的网页。
